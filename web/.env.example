# Supabase — safe to expose to the browser (RLS is what protects the data)
NEXT_PUBLIC_SUPABASE_URL=https://egaaigoplqinjvwtnhoo.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# NEVER put SUPABASE_SERVICE_ROLE_KEY in this app.
# The service role bypasses RLS; it belongs only in Edge Functions.
