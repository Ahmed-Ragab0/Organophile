# deps
node_modules/
.pnpm-store/

# builds
.next/
out/
dist/
coverage/
*.tsbuildinfo

# env — never commit real secrets
.env
.env.local
.env.*.local
!.env.example

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# supabase local
supabase/.branches
supabase/.temp
supabase/functions/.temp

# generated secrets — never commit
.secrets/

# local data snapshots — never commit
.backups/
